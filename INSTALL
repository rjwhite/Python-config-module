# copy distribution to some make directory

% mkdir /tmp/config
% cp dist/config-3.1.tar.gz   /tmp/config
% cd /tmp/config

# uncompress and expand

% gunzip config-3.1.tar.gz
% tar xf config-3.1.tar
% cd config-3.1

# install as root
# should be installed someplace like /usr/local//lib/python2.7/dist-packages

% sudo python setup.py install

